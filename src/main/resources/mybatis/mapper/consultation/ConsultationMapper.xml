<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.team.independence.consultation.mapper.ConsultationMapper">

    <resultMap id="reservationMap" type="com.team.independence.consultation.domain.ConsultationReservation">
        <id     property="reservationId"   column="reservation_id"/>
        <result property="userId"          column="user_id"/>
        <result property="counselorId"     column="counselor_id"/>
        <result property="consultationType" column="consultation_type"
                javaType="com.team.independence.consultation.domain.ConsultationType"/>
        <result property="category"        column="category"
                javaType="com.team.independence.consultation.domain.ConsultationCategory"/>
        <result property="reservationDate" column="reservation_date"/>
        <result property="reservationTime" column="reservation_time"/>
        <result property="requestMessage"  column="request_message"/>
        <result property="consultInfoJson" column="consult_info_json"/>
        <result property="diagnosisJson"   column="diagnosis_json"/>
        <result property="reportJson"      column="report_json"/>
        <result property="reportStatus"    column="report_status"
                javaType="com.team.independence.consultation.domain.ReportStatus"/>
        <result property="status"          column="status"
                javaType="com.team.independence.consultation.domain.ConsultationStatus"/>
        <result property="createdAt"       column="created_at"/>
        <result property="endedAt"         column="ended_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.team.independence.consultation.domain.ConsultationReservation"
            useGeneratedKeys="true" keyProperty="reservationId">
        INSERT INTO consultation_reservation (
            user_id, counselor_id, consultation_type, category,
            reservation_date, reservation_time, request_message,
            consult_info_json, diagnosis_json, status
        ) VALUES (
            #{userId}, #{counselorId}, #{consultationType}, #{category},
            #{reservationDate}, #{reservationTime}, #{requestMessage},
            #{consultInfoJson}, #{diagnosisJson}, #{status}
        )
    </insert>

    <select id="findByUserId" resultMap="reservationMap">
        SELECT reservation_id, user_id, counselor_id, consultation_type, category,
               reservation_date, reservation_time, request_message,
               consult_info_json, diagnosis_json, status, created_at, ended_at
        FROM consultation_reservation
        WHERE user_id = #{userId}
        ORDER BY reservation_date DESC, reservation_time DESC
    </select>

    <resultMap id="counselorReservationMap"
               type="com.team.independence.consultation.dto.ConsultationCounselorReservationResponse">
        <id     property="reservationId"   column="reservation_id"/>
        <result property="userId"          column="user_id"/>
        <result property="userName"        column="user_name"/>
        <result property="consultationType" column="consultation_type"/>
        <result property="category"        column="category"/>
        <result property="reservationDate" column="reservation_date"/>
        <result property="reservationTime" column="reservation_time"/>
        <result property="status"          column="status"/>
    </resultMap>

    <select id="findByCounselorId" resultMap="counselorReservationMap">
        SELECT cr.reservation_id, cr.user_id, m.nickname AS user_name, cr.consultation_type,
               cr.category, cr.reservation_date, cr.reservation_time, cr.status
        FROM consultation_reservation cr
        JOIN member m ON m.id = cr.user_id
        WHERE cr.counselor_id = #{counselorId}
        ORDER BY cr.reservation_date ASC, cr.reservation_time ASC
    </select>

    <select id="findById" resultMap="reservationMap">
        SELECT reservation_id, user_id, counselor_id, consultation_type, category,
               reservation_date, reservation_time, request_message,
               consult_info_json, diagnosis_json, report_json, report_status,
               status, created_at, ended_at
        FROM consultation_reservation
        WHERE reservation_id = #{reservationId}
    </select>

    <update id="updateStatusToInProgress">
        UPDATE consultation_reservation
           SET status = 'IN_PROGRESS'
         WHERE reservation_id = #{reservationId}
           AND status         = 'RESERVED'
    </update>

    <update id="updateStatusToCompleted">
        UPDATE consultation_reservation
           SET status   = 'COMPLETED',
               ended_at = NOW()
         WHERE reservation_id = #{reservationId}
           AND status IN ('RESERVED', 'IN_PROGRESS')
    </update>

    <update id="updateReport">
        UPDATE consultation_reservation
           SET report_status = #{reportStatus},
               report_json   = #{reportJson}
         WHERE reservation_id = #{reservationId}
    </update>

</mapper>
