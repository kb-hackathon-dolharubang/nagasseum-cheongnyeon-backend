<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.team.independence.property.mapper.RegionDongMapper">

    <select id="findBySigunguCode" resultType="com.team.independence.property.domain.RegionDong">
        SELECT code, sigungu_code, dong_name, full_name
          FROM region_dong
         WHERE sigungu_code = #{sigunguCode}
         ORDER BY dong_name
    </select>

    <select id="findByCode" resultType="com.team.independence.property.domain.RegionDong">
        SELECT code, sigungu_code, dong_name, full_name
          FROM region_dong
         WHERE code = #{code}
    </select>

</mapper>
