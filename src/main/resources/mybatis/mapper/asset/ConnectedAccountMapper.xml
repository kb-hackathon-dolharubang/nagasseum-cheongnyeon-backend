<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.team.independence.asset.mapper.ConnectedAccountMapper">

    <resultMap id="connectedAccountMap" type="com.team.independence.asset.domain.codef.ConnectedAccount">
        <id     property="id"              column="id"/>
        <result property="memberId"        column="member_id"/>
        <result property="connectedId"     column="connected_id"/>
        <result property="birthDate"       column="birth_date"/>
        <result property="connectedStatus" column="connected_status"/>
        <result property="lastSyncedAt"    column="last_synced_at"/>
        <result property="syncStatus"      column="sync_status"/>
        <result property="createdAt"       column="created_at"/>
        <result property="updatedAt"       column="updated_at"/>
    </resultMap>

    <select id="findByMemberId" parameterType="long" resultMap="connectedAccountMap">
        SELECT id, member_id, connected_id, birth_date, connected_status,
               last_synced_at, sync_status, created_at, updated_at
          FROM connected_account
         WHERE member_id = #{memberId}
    </select>

    <select id="findAllMemberIds" resultType="java.lang.Long">
        SELECT member_id FROM connected_account WHERE connected_status = 'ACTIVE'
    </select>

    <insert id="insert" parameterType="com.team.independence.asset.domain.codef.ConnectedAccount"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO connected_account (member_id, connected_id, birth_date, connected_status)
        VALUES (#{memberId}, #{connectedId}, #{birthDate}, 'ACTIVE')
    </insert>

    <update id="updateSyncStatus" parameterType="com.team.independence.asset.domain.codef.ConnectedAccount">
        UPDATE connected_account
           SET sync_status    = #{syncStatus},
               last_synced_at = #{lastSyncedAt}
         WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="long">
        DELETE FROM connected_account WHERE id = #{id}
    </delete>

</mapper>
